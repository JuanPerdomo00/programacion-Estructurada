#include <stdio.h>
#include <stdlib.h>

int main()
{
    int suma, modulo;
    float res, div, mult;

    suma = 2 + 10;
    res = 3.2 - 2.1;
    mult = 7 * 3;
    div = 6.3 / 3.0;
    modulo = 3 % 2;

    printf("suma: %i\n, resta: %f\n, multiplicacion: %f\n, divicion: %f\n, modulo: %i\n", suma,res,mult,div, modulo);


    return 0;
}
